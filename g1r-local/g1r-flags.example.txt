# G1R Mod — lokale Flag-Overrides (OPTIONAL)
#
# Die Defaults in main.lua sind schon richtig gesetzt:
#   AN:  READ_ATTACK, READ_CLOCK, READ_CARRY (Waffe/in-hand), READ_COMBO
#   AUS: READ_SPELL, READ_WEAPONS (crashen hart), READ_KILLS (tote Map),
#        READ_KILLS_HOOK, READ_DMG_OUT (ungetestet)
#
# Du brauchst diese Datei NUR, wenn du etwas anders willst als die Defaults —
# z.B. einen ungetesteten Reader einzeln scharf schalten, ohne main.lua anzufassen.
# Dann nach  C:\obs-g1r\g1r-flags.txt  kopieren. Pull/Kopieren der main.lua aendert
# diese Datei NICHT. Format: KEY=true/false je Zeile, # = Kommentar.

# Beispiel: den Kill-Hook zum Testen einzeln aktivieren
# READ_KILLS_HOOK=true

# Beispiel: ausgeteilten Schaden (melee) testen
# READ_DMG_OUT=true

# NIE auf true setzen (crasht hart am aktuellen Build):
# READ_SPELL / READ_WEAPONS
